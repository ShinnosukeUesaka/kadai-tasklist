<h1>
<%= @task.content %>
</h1>

<%= form_with(model: @task, local: true) do |f| %>

<%= f.label :content, 'タスク編集' %>

<%= f.text_field :content %>


<%= f.submit '投稿' %>

<% end %>


<%= link_to 'タスク一覧', tasks_path %>